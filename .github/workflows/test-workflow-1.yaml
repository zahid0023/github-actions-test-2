name: Test Workflow 1

on: push

jobs:
    test-job-1:
        runs-on: ubuntu-latest
        steps:
            - name: Hello 1
              run: echo "Hello 1!"
    
    test-job-2:
        runs-on: ubuntu-latest
        steps:
            - name: Exit Step
              run: exit 1;

            - name: Hello 2
              run: echo "Hello 2!";